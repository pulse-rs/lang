use { println, eprintln } from "std::io";

export fn main(arg: int) -> int {
    let i = 3.14;
    let j = true;

    if j && i > 3.0 {
        println("Hello, world!");
    } else {
        eprintln("Goodbye, world!");
    }

    return 0;
}
